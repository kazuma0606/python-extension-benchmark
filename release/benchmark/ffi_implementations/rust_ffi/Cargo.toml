[package]
name = "rust_ffi_functions"
version = "0.1.0"
edition = "2021"

[lib]
name = "librustfunctions"
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"